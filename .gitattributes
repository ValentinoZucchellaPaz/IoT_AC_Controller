# Normalize line endings for all text files
* text=auto

# Force LF for shell scripts (hooks must use LF on all platforms)
*.sh text eol=lf
.husky/commit-msg text eol=lf
.husky/pre-commit text eol=lf

# Force CRLF for Windows batch files
*.bat text eol=crlf
*.cmd text eol=crlf
