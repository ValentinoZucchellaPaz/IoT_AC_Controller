# Configuración de Node.js

Este proyecto requiere **Node.js 22 LTS**. Se recomienda usar [nvm](https://github.com/nvm-sh/nvm).

```bash
nvm install    # instala la versión del .nvmrc
nvm use        # cambia a ella
node --version # debe mostrar v22.x.x
```
